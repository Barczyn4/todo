// Generated by CoffeeScript 1.3.3
var help, program;

program = require('commander');

program.command('new [name]').description('create a new app and/or extension').usage('[name] [options]').option('-t, --template [name]', 'template to use').option('-c, --config [config file]', 'config file to use').action(function(name, opts) {
  if (opts == null) {
    opts = {};
  }
  if (!name) {
    return console.log('Name of project required');
  }
  return require('./generator')(name, opts);
});

help = function() {
  return console.log(program.helpInformation());
};

program.parse(process.argv);

if (!program.args.length) {
  help();
}
